const textareaTemplate: string = `
  <textarea id='{{id}}' class='{{className}}' rows='{{rows}}' cols='{{cols}}' maxLength='{{length}}' name='{{name}}' placeholder='{{placeholder}}'></textarea>
`;
export default textareaTemplate;
